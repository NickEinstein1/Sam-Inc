import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SAM INC - Professional Tax & Financial Services',
  description: 'Leading provider of tax consultancy, financial auditing, data analytics, and AI solutions. Professional services you can trust.',
  keywords: 'tax consultancy, financial auditing, data analytics, artificial intelligence, business consulting',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
